# API Server Dependencies — pinned for reproducible deploys
# Generated: 2026-03-18
#
# Note: pandas is intentionally excluded from the API server install. Pipeline
# scripts that need it (e.g. scripts/player_weekly_roster.py) install separately:
#   pip install pandas==2.2.3
# nflreadpy (nflverse rosters) is also pipeline-only:
#   pip install nflreadpy

# Web framework
fastapi==0.116.0
uvicorn[standard]==0.35.0
gunicorn==23.0.0

# Rate limiting
slowapi==0.1.9

# Data processing (API layer)
polars==1.30.0
duckdb==1.3.1

# Terminal table output (scripts/view_rankings.py)
prettytable==3.17.0

# Data validation
pydantic==2.11.7

# Scraping (pipeline scripts only — included for run_pipelines.py)
beautifulsoup4==4.12.3
requests==2.32.3

# Image processing (headshot pipeline writes .jpg)
# Kept <12 for compatibility with streamlit dependency constraints in this repo.
Pillow==11.3.0

# Database (PostgreSQL — scaffolded)
psycopg[binary]==3.2.4
psycopg-pool==3.2.4

# Config & templates
python-dotenv==1.0.1
jinja2==3.1.5

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
email-validator==2.2.0
# Database
psycopg[binary]>=3.0.0
psycopg_pool>=3.0.0

